# Audit Cartera v4.0 — commit `2dbf950`

**"Extraer _build_risk_focus_block y añadir tests unitarios para decision/actions"**
Fecha: 2026-04-24 · Suite: **317/317 OK**

---

## 0. Diagnóstico ejecutivo

Commit de deuda técnica limpio: cierra dos ítems del plan de acción anterior. `_build_risk_focus_block` se extrae de su posición anidada dentro de `build_summary_section` al nivel de módulo en `report_sections.py` (sin cambio de lógica, refactor puro). Se crea `test_decision_actions.py` (204 ln, 19 tests nuevos) cubriendo `assign_base_action`, `assign_action_v2` y `enrich_decision_explanations`. El archivo de audit anterior queda archivado en `docs/archive/`. 317/317 verde.

---

## 1. Cinco secciones × /10

| # | Sección | Nota | Δ vs 8284e87 |
|---|---------|------|-------------|
| S1 | Arquitectura y diseño | **9.3** | +0.1 |
| S2 | Corrección y robustez | **9.5** | = |
| S3 | Testing | **9.3** | +0.2 |
| S4 | Observabilidad y mantenibilidad | **9.0** | +0.3 |
| S5 | Gestión de riesgos | **9.2** | = |
| — | **Total** | **9.26** | **+0.12** |

**S1 — Arquitectura y diseño: 9.3**
`_build_risk_focus_block` es ahora una función de módulo con visibilidad clara, testeable de forma independiente. `report_sections.py` queda en 817 ln — el refactor fue neutral en tamaño pero correcto en organización. `test_decision_actions.py` está bien estructurado con dos clases separadas: `AssignBaseActionTests` y `EnrichDecisionExplanationsTests`. Pendiente: `_build_risk_focus_block` no tiene test unitario propio a pesar de ser ahora extraíble.

**S2 — Corrección y robustez: 9.5**
Extracción pura sin cambio de lógica — riesgo de regresión mínimo, confirmado por 317/317. Los tests de `actions.py` verifican correctamente los invariantes de negocio críticos: bonos y liquidez no pueden recibir `ACTION_REFUERZO` ni `ACTION_REDUCIR`, los thresholds son configurables vía `action_rules`, y `ACTION_DESPLEGAR_LIQUIDEZ` solo se asigna cuando el score de despliegue supera umbral.

**S3 — Testing: 9.3**
317/317 verde (+19 tests). `test_decision_actions.py` cubre 8 escenarios de `assign_base_action` (refuerzo dominante, reducción dominante, empate dentro de gap, score bajo umbral, bono excluido, liquidez excluida, despliegue de liquidez, reglas custom), `assign_action_v2` con columnas `_v2`, y múltiples filas en un solo DataFrame. O-007 cerrado para `actions.py`. **Gaps que persisten**: `decision/scoring.py` (885 ln) sin tests directos; escenario `serie_confiable=False` ausente en `test_portfolio_risk.py`; `EXCLUDED_RISK_TICKERS/TYPES` sin cobertura unitaria.

**S4 — Observabilidad y mantenibilidad: 9.0**
La extracción mejora la legibilidad de `build_summary_section` — ya no tiene funciones anidadas de 40+ líneas. El archivo `docs/archive/audit-2026-04-24-8284e87.md` establece un patrón de trazabilidad de auditorías. `report_sections.py` estable en 817 ln. Puntos pendientes: `test_report_render.py` sigue sin partirse (O-006), `generate_real_report.py` en 945 ln.

**S5 — Gestión de riesgos: 9.2**
Refactor puro con suite completa verde — riesgo prácticamente nulo. Los tests de `actions.py` actúan ahora como red de seguridad ante futuros cambios en la lógica de asignación de acciones, que es el punto de mayor impacto en las recomendaciones al usuario.

---

## 2. Matriz de hallazgos

| ID | Tipo | Archivo | Descripción |
|----|------|---------|-------------|
| ✅ O-007 | **CERRADO (parcial)** | `tests/test_decision_actions.py` | 204 ln, 19 tests para `actions.py` |
| ✅ O-008 | **CERRADO** | `scripts/report_sections.py` | `_build_risk_focus_block` extraída a nivel de módulo |
| O-006 | Persiste | `test_report_render.py` | ~1070+ ln sin split |
| O-009 | Persiste | `test_portfolio_risk.py` | Falta test `serie_confiable=False` y exclusión de CAUCION/FCI |
| O-007b | Persiste | `decision/scoring.py` | 885 ln sin tests unitarios directos |
| O-011 | Oportunidad | `scripts/report_sections.py` | `_build_risk_focus_block` ahora extraíble — candidato a test unitario propio |

---

## 3. Veredicto

**9.26 / 10** — Nueva nota histórica máxima. El proyecto demuestra madurez en el ciclo audit → deuda → cierre: los dos ítems "Alta" del último plan de acción fueron implementados en el commit siguiente. La deuda visible se reduce a tres frentes menores: cobertura de `scoring.py`, tests de edge cases en `portfolio_risk.py`, y el split pendiente de `test_report_render.py`.

---

## 4. Plan de acción

| Prioridad | Acción | Archivo destino |
|-----------|--------|-----------------|
| Alta | Añadir tests para `serie_confiable=False` (cobertura < 85% entre snapshots → métricas NaN) y exclusión de CAUCION/FCI del universo de riesgo | `tests/test_portfolio_risk.py` |
| Alta | Crear `tests/test_decision_scoring.py` — smoke tests de `compute_scores` con portfolio mínimo (O-007b) | `tests/test_decision_scoring.py` |
| Media | Añadir test unitario para `_build_risk_focus_block` directamente (ahora es testeable desde el módulo) | `tests/test_report_sections.py` |
| Baja | Split `test_report_render.py` en sub-archivos por dominio cuando supere 1200 ln (O-006) | `tests/` |
